apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yml: |
    scrape_configs:
    - job_name: 'backend'
      scrape_interval: 15s
      metrics_path: /metrics
      static_configs:
      - targets: ['10.104.95.31:3002'] # Usando el nombre del servicio